import { CheckerIdentifySkeleton } from "@/components/checker/CheckerSkeletons";

/** Каркас главного экрана: возврат на вкладку «Распознать» тоже мгновенный. */
export default function Loading() {
  return <CheckerIdentifySkeleton />;
}
